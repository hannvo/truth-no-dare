<button class="btn-back" data-action="click->new-question#closeModal">back</button>

<div class="modal-card">
  <h2 class="<%= "title-#{@board.theme}" %>">New Question</h2>
  <%= render 'shared/text_form', { parent: @board, type: @question } %>
</div>
